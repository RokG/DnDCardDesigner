<UserControl x:Class="CardDesigner.UI.Controls.DataLabel" x:Name="dataLabel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CardDesigner.UI.Controls">
    <StackPanel Orientation="Horizontal">
        <Label
            Content="{Binding ValueName, ElementName=dataLabel}"
            ContentStringFormat="{}{0}: " />
        <Label Content="{Binding Value1, ElementName=dataLabel}" />
        <Label Content="{Binding Value2, ElementName=dataLabel}" />
        <Label Content="{Binding Value3, ElementName=dataLabel}" />
    </StackPanel>
</UserControl>
