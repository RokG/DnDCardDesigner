<UserControl x:Class="CardDesigner.UI.Views.ItemCardView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:CardDesigner.UI.Controls"
    xmlns:local="clr-namespace:CardDesigner.UI.Views"
    BorderBrush="{StaticResource WindowBorder}"
    BorderThickness="3,0,0,0">
    <Grid Margin="4">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  header area  -->
        <Border BorderThickness="0,0,0,1">
            <StackPanel>
                <Label
                    Content="{Binding Name}"
                    ContentStringFormat="{}{0}s"
                    FontSize="32" />
                <Label Content="{Binding Description}" />
            </StackPanel>
        </Border>

        <!--  Content Area  -->
        <Grid Grid.Row="1">
            <StackPanel
                Grid.Row="0"
                Orientation="Horizontal">
                <Label Content="{Binding CardName}" />
                <Label Content="Item card background" />
                <ComboBox SelectedIndex="0">
                    <ComboBoxItem Content="StyleMagicItems" />
                    <ComboBoxItem Content="StylePotions" />
                    <ComboBoxItem Content="StyleCustomItems1" />
                </ComboBox>


            </StackPanel>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <StackPanel
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <Label Content="Item type" />
                        <ComboBox SelectedIndex="0">
                            <ComboBoxItem Content="MagicItems" />
                            <ComboBoxItem Content="Potions" />
                            <ComboBoxItem Content="CustomItems1" />
                        </ComboBox>
                    </StackPanel>
                    <StackPanel
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <Label Content="Card Title" />
                        <TextBox Width="100" />
                    </StackPanel>
                    <StackPanel
                        VerticalAlignment="Center"
                        Orientation="Horizontal">
                        <Label Content="Card Description" />
                        <TextBox Width="100" />
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <Viewbox Margin="10">
                        <Canvas
                            Width="571"
                            Height="889"
                            Margin="20" />
                    </Viewbox>
                </StackPanel>
            </Grid>
        </Grid>

    </Grid>
</UserControl>