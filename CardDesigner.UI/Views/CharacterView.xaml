<UserControl
    x:Class="CardDesigner.UI.Views.CharacterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CardDesigner.UI.Views">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="9*" />
        </Grid.RowDefinitions>

        <!--  header area  -->
        <Border
            BorderBrush="Black"
            BorderThickness="1">
            <Label
                Content="Character overview"
                FontSize="36" />
        </Border>

        <!--  body area  -->
        <Border Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--  Character overview  -->
                <StackPanel Orientation="Vertical">
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Character: " />
                        <ComboBox
                            Width="100"
                            Height="40"
                            DisplayMemberPath="Name"
                            ItemsSource="{Binding AllCharacters}"
                            SelectedItem="{Binding SelectedCharacter}" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="Spell deck: " />
                        <Label Content="{Binding SelectedSpellDeck.Name}" />
                    </StackPanel>
                    <Label Content="Cards in deck : " />
                    <ListBox
                        Grid.Row="2"
                        ItemsSource="{Binding SelectedSpellDeckCards}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Label Content="{Binding Name}" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

                <!--  Card overview  -->
                <TabControl Grid.Column="1">
                    <TabItem Header="Card back side">
                        <StackPanel Background="blue">
                            <Label Content="Character Cards back side" />
                            <ComboBox SelectedIndex="0">
                                <ComboBoxItem Content="DruidSpells" />
                                <ComboBoxItem Content="WizardSpells" />
                                <ComboBoxItem Content="CustomBackground1" />
                            </ComboBox>

                        </StackPanel>
                    </TabItem>
                    <TabItem Header="Card Front side" />
                    <TabItem Header="Card Front side background" />
                </TabControl>

            </Grid>
        </Border>
    </Grid>
</UserControl>
